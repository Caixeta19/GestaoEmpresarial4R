package com.vivo4redes.syscor.enums;

/**
 * Categoria do item dentro do carrinho de venda — corresponde 1:1 às abas
 * da tela de registro de venda (Produto Vivo / Serviço Vivo / Recarga).
 * Uma mesma Venda pode ter itens de categorias diferentes misturados.
 */
public enum CategoriaItemVenda {
    PRODUTO_VIVO,
    SERVICO_VIVO,
    RECARGA
}
